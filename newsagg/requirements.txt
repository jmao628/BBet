# newsagg collection-layer dependencies
# (RSS/Atom parsing uses the stdlib — no feedparser/sgmllib3k needed.)
httpx>=0.27
beautifulsoup4>=4.12
PyYAML>=6.0
# Optional — only needed when schwab_youtube.fetch_transcripts is true:
yt-dlp>=2024.1
